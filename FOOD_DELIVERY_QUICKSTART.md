# 🍔 Food Delivery App - Quick Start Guide

## ✅ สิ่งที่สร้างแล้ว:

### 1. **Database Structure** 
   - Firestore collections: restaurants, menus, orders, users
   - Type definitions ใน `types/food-delivery.ts`

### 2. **Services**
   - **restaurantService.ts** - จัดการข้อมูลร้าน & เมนู
   - **orderService.ts** - จัดการคำสั่งซื้อ

### 3. **Screens**
   - **restaurants.tsx** - หน้าแสดงรายการร้านอาหาร (รอสร้าง route)

### 4. **Sample Data**
   - `scripts/seedData.ts` - ข้อมูลร้านอาหารตัวอย่าง 6 ร้าน

## 🚀 วิธีเริ่มต้น:

### Step 1: เพิ่มข้อมูลตัวอย่าง

1. เปิดแอป และไปที่หน้า **Profile**
2. กดปุ่ม **"เพิ่มข้อมูลตัวอย่าง"**
3. รอจนเสร็จ จะมีข้อความ "เพิ่มข้อมูลเรียบร้อย!"

หรือรันคำสั่งนี้:
\`\`\`typescript
import { seedDatabase } from './scripts/seedData';
seedDatabase().then(() => console.log('Done!'));
\`\`\`

### Step 2: ตรวจสอบใน Firebase Console

1. เปิด [Firebase Console](https://console.firebase.google.com/)
2. เลือกโปรเจ็กต์ **customer-18261**
3. ไปที่ **Firestore Database**
4. ดู collections:
   - **restaurants** - จะมี 6 ร้าน
   - **restaurants/{id}/menus** - แต่ละร้านมีเมนู

### Step 3: เพิ่ม Tab ร้านอาหาร

เพิ่มใน `app/(main)/_layout.tsx`:

\`\`\`tsx
<Tabs.Screen
  name="restaurants"
  options={{
    title: 'ร้านอาหาร',
    tabBarIcon: ({ color, focused }) => (
      <TabBarIcon name={focused ? 'restaurant' : 'restaurant-outline'} color={color} />
    ),
  }}
/>
\`\`\`

## 📋 Features ที่ต้องสร้างต่อ:

### ✅ ทำแล้ว:
- [x] Database structure & types
- [x] Restaurant & Menu services
- [x] Order service  
- [x] Restaurant list screen
- [x] Sample data script

### 📝 ต้องทำต่อ:
- [ ] Restaurant detail screen (แสดงเมนู)
- [ ] Cart screen (ตะกร้าสินค้า)
- [ ] Checkout flow
- [ ] Order history screen
- [ ] Order tracking (แสดงสถานะ)
- [ ] User addresses management

## 🎯 ขั้นตอนต่อไป:

### 1. สร้างหน้ารายละเอียดร้าน
สร้าง `app/(main)/restaurant/[id].tsx`

### 2. สร้างหน้าตะกร้า
สร้าง `app/(main)/cart.tsx`

### 3. สร้าง Context สำหรับ Cart
สร้าง `contexts/CartContext.tsx`

### 4. สร้างหน้าประวัติคำสั่งซื้อ
สร้าง `app/(main)/orders/index.tsx`

## 🐛 การแก้ปัญหา:

### หากไม่มีข้อมูลแสดง:
1. ตรวจสอบใน Firebase Console ว่ามีข้อมูลหรือไม่
2. ลองกดปุ่ม "โหลดใหม่"
3. เช็ค Console log ว่ามี error อะไร

### หาก Firestore Rules บล็อก:
ใน Firebase Console → Firestore → Rules:

\`\`\`
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
\`\`\`

## 📱 ทดสอบ:

1. Login เข้าแอป
2. ไปหน้า Profile → กดเพิ่มข้อมูลตัวอย่าง
3. ไปหน้า Restaurants (หรือ Home)
4. ดูรายการร้านอาหาร
5. ทดสอบค้นหา และ filter ตามหมวดหมู่

---

**ต่อไปจะสร้างหน้ารายละเอียดร้านและระบบตะกร้าครับ!** 🚀
